import{index,integer,real,sqliteTable,text}from"drizzle-orm/sqlite-core";
export const runs=sqliteTable("runs",{id:text("id").primaryKey(),storyUrl:text("story_url").notNull(),title:text("title").notNull(),keyword:text("keyword").notNull(),status:text("status").notNull(),visiblePosts:integer("visible_posts").notNull().default(0),reportedPosts:text("reported_posts").notNull().default("unknown"),totalImpressions:integer("total_impressions").notNull().default(0),likes:integer("likes").notNull().default(0),replies:integer("replies").notNull().default(0),reposts:integer("reposts").notNull().default(0),sparkPosts:integer("spark_posts").notNull().default(0),sparkImpressions:integer("spark_impressions").notNull().default(0),coveragePercent:real("coverage_percent").notNull().default(0),artifactPrefix:text("artifact_prefix"),createdAt:text("created_at").notNull(),completedAt:text("completed_at")},table=>[index("idx_runs_created_at").on(table.createdAt),index("idx_runs_status").on(table.status)]);
